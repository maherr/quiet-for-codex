[project]
name = "codex-scripts"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = ["psutil>=7.2.2", "rich>=14.3.1", "ruff>=0.15.8"]

[tool.uv]
exclude-newer = "7 days"
index-strategy = "first-index"
